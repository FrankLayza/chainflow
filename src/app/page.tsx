'use client';

export default function Home() {
  return (
    <main className="flex flex-col h-screen w-screen bg-deep-ember text-warm-off-white overflow-hidden font-sans">
      {/* Blank canvas for new UI */}
    </main>
  );
}
